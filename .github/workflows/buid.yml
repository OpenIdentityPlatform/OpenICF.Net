name: Build 

on:
  push:
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: windows-2019

    steps:
      - uses: actions/checkout@v4

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2
  
      - name: setup-msbuild
        uses: microsoft/setup-msbuildv@2

      - name: Restore Packages
        run: |
          nuget restore ConnectorFramework.sln
          nuget restore dotnet-connector\DotNetConnectors.sln
          nuget restore powershell-connector\PowerShellConnector.sln
          
      - name: Build solution
        run: |
          msbuild ConnectorFramework.sln -t:rebuild -property:Configuration=Release
          msbuild dotnet-connector\DotNetConnectors.sln -t:rebuild -property:Configuration=Release
          msbuild powershell-connector\PowerShellConnector.sln -t:rebuild -property:Configuration=Release
